ADMIN_EMAIL = "admin@example.com"
ADMIN_PASSWORD = "replace-with-strong-password"
ADMIN_FULL_NAME = "Admin"
ADMIN_ORGANIZATION = "SALA"
AUTH_PERSIST_SECRET = "replace-with-a-long-random-secret"

# Optional: enables email notifications for the access-request workflow
# (new-request alert to ADMIN_EMAIL, approval email to the new user). If
# SMTP_HOST is left unset, these notifications are silently skipped and the
# app keeps working exactly as before.
SMTP_HOST = ""
SMTP_PORT = "587"
SMTP_USER = ""
SMTP_PASSWORD = ""
SMTP_FROM_EMAIL = ""
SMTP_USE_TLS = "true"
APP_URL = "https://app.sala-global.com"

